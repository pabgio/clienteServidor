import {model, Schema} from 'mongoose';

export const Usuario = model("Usuario",
 new Schema({
    nome: {
        type: String,
        required: true
    },
    email: {
        type: String,
        required: true
    },
    senha: {
        type: String,
        required: true
    },
}));